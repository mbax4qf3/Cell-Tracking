# Cell-Tracking

The project is to detect and track cells as well as detecting mitosis from three image datasets.

Each dataset is implemented via a src file.

A UI program is developed to get the paticular cell's motion info.

Algorithm and evaluation are described in report.
